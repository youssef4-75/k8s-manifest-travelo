apiVersion: v1
kind: Service
metadata:
  name: travelo-frontend-service
  namespace: travelo
spec:
  type: NodePort
  selector:
    app: travelo-frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30000