apiVersion: v1
kind: Service
metadata:
  name: travelo-backend-service
  namespace: travelo
spec:
  type: ClusterIP
  selector:
    app: travelo-backend
  ports:
  - port: 8080
    targetPort: 8080
    name: http
